{
  "name": "marklingo",
  "displayName": "MarkLingo - Translate Markdown with AI",
  "description": "Translate Markdown with OpenRouter, OpenAI, DeepSeek, Moonshot, GLM, Xiaomi MiMo, and custom endpoints without breaking Markdown.",
  "version": "0.0.4",
  "publisher": "zzgosh",
  "type": "module",
  "license": "SEE LICENSE IN LICENSE",
  "icon": "resources/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/zzgosh/marklingo.git"
  },
  "bugs": {
    "url": "https://github.com/zzgosh/marklingo/issues"
  },
  "homepage": "https://github.com/zzgosh/marklingo#readme",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "markdown",
    "translate",
    "translation",
    "ai",
    "openrouter",
    "i18n",
    "localization"
  ],
  "galleryBanner": {
    "color": "#0f172a",
    "theme": "dark"
  },
  "main": "./dist/extension.js",
  "extensionKind": [
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "MarkLingo reads Markdown documents and can send their content to a configured translation endpoint, so the workspace should be trusted before translation runs."
    },
    "virtualWorkspaces": {
      "supported": false,
      "description": "MarkLingo currently writes translated files using local filesystem paths and does not support virtual workspace file systems."
    }
  },
  "contributes": {
    "commands": [
      {
        "command": "marklingo.translateCurrentMarkdown",
        "title": "MarkLingo: Translate Current Markdown"
      },
      {
        "command": "marklingo.translateCurrentMarkdownFull",
        "title": "MarkLingo: Retranslate Current Markdown"
      },
      {
        "command": "marklingo.translateExplorerMarkdownFile",
        "title": "MarkLingo: Translate This Markdown File"
      },
      {
        "command": "marklingo.translateSelectedMarkdownResources",
        "title": "MarkLingo: Translate Selected Markdown Files"
      },
      {
        "command": "marklingo.translateFolderMarkdown",
        "title": "MarkLingo: Translate All Markdown in This Folder"
      },
      {
        "command": "marklingo.deleteCurrentProjectTranslatedFiles",
        "title": "MarkLingo: Delete Current Project Translated Files"
      },
      {
        "command": "marklingo.ignoreTranslatedFilesInGit",
        "title": "MarkLingo: Add Translated Files to .git/info/exclude"
      },
      {
        "command": "marklingo.openSettings",
        "title": "MarkLingo: Open Settings"
      }
    ],
    "keybindings": [
      {
        "command": "marklingo.translateCurrentMarkdown",
        "key": "ctrl+alt+t",
        "mac": "alt+cmd+t",
        "win": "ctrl+alt+t",
        "linux": "ctrl+alt+t",
        "when": "editorTextFocus && (editorLangId == markdown || resourceExtname == .md || resourceExtname == .markdown)"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "marklingo.translateCurrentMarkdown",
          "when": "!(resourceFilename =~ /_mdt[.](md|markdown)$/i) && (editorLangId == markdown || resourceExtname == .md || resourceExtname == .markdown)",
          "group": "marklingo@10"
        }
      ],
      "explorer/context": [
        {
          "command": "marklingo.translateExplorerMarkdownFile",
          "when": "!listMultiSelection && !explorerResourceIsFolder && isFileSystemResource && !(resourceFilename =~ /_mdt[.](md|markdown)$/i) && (resourceExtname == .md || resourceExtname == .markdown)",
          "group": "marklingo@10"
        },
        {
          "command": "marklingo.translateSelectedMarkdownResources",
          "when": "listMultiSelection && isFileSystemResource && !(resourceFilename =~ /_mdt[.](md|markdown)$/i) && (explorerResourceIsFolder || resourceExtname == .md || resourceExtname == .markdown)",
          "group": "marklingo@10"
        },
        {
          "command": "marklingo.translateFolderMarkdown",
          "when": "!listMultiSelection && explorerResourceIsFolder && isFileSystemResource",
          "group": "marklingo@11"
        }
      ],
      "commandPalette": [
        {
          "command": "marklingo.translateExplorerMarkdownFile",
          "when": "false"
        },
        {
          "command": "marklingo.translateSelectedMarkdownResources",
          "when": "false"
        },
        {
          "command": "marklingo.translateFolderMarkdown",
          "when": "false"
        }
      ]
    },
    "configuration": {
      "title": "MarkLingo",
      "properties": {
        "marklingo.openrouter.provider": {
          "type": "string",
          "default": "openrouter",
          "enum": [
            "openrouter",
            "openai",
            "deepseek",
            "moonshot",
            "glm",
            "xiaomiMimo",
            "openaiCompatible"
          ],
          "enumDescriptions": [
            "Use OpenRouter's official API endpoint.",
            "Use OpenAI's official Chat Completions endpoint.",
            "Use DeepSeek's OpenAI-compatible API endpoint.",
            "Use Moonshot's OpenAI-compatible API endpoint.",
            "Use GLM's OpenAI-compatible API endpoint.",
            "Use Xiaomi MiMo's OpenAI-compatible API endpoint.",
            "Use a custom OpenAI-compatible API endpoint, such as llama.cpp server, Ollama, or LM Studio."
          ],
          "description": "Provider used for translation requests."
        },
        "marklingo.openrouter.baseUrl": {
          "type": "string",
          "scope": "application",
          "default": "https://openrouter.ai/api/v1",
          "description": "Active provider base URL kept for compatibility. MarkLingo Settings remembers Custom OpenAI Compatible Base URL separately."
        },
        "marklingo.openrouter.modelId": {
          "type": "string",
          "default": "google/gemini-3.1-flash-lite",
          "description": "Active provider model ID kept for compatibility. Prefer MarkLingo Settings for provider-specific model values."
        },
        "marklingo.providers.openrouter.modelId": {
          "type": "string",
          "default": "google/gemini-3.1-flash-lite",
          "description": "Model ID remembered for the OpenRouter provider."
        },
        "marklingo.providers.openai.modelId": {
          "type": "string",
          "default": "",
          "description": "Model ID remembered for the OpenAI provider."
        },
        "marklingo.providers.deepseek.modelId": {
          "type": "string",
          "default": "",
          "description": "Model ID remembered for the DeepSeek provider."
        },
        "marklingo.providers.moonshot.modelId": {
          "type": "string",
          "default": "",
          "description": "Model ID remembered for the Moonshot provider."
        },
        "marklingo.providers.glm.modelId": {
          "type": "string",
          "default": "",
          "description": "Model ID remembered for the GLM provider."
        },
        "marklingo.providers.xiaomiMimo.modelId": {
          "type": "string",
          "default": "",
          "description": "Model ID remembered for the Xiaomi MiMo provider."
        },
        "marklingo.providers.openaiCompatible.baseUrl": {
          "type": "string",
          "scope": "application",
          "default": "",
          "description": "Base URL remembered for the Custom OpenAI Compatible provider."
        },
        "marklingo.providers.openaiCompatible.modelId": {
          "type": "string",
          "default": "",
          "description": "Model ID remembered for the Custom OpenAI Compatible provider."
        },
        "marklingo.translation.maxBlocksPerRequest": {
          "type": "number",
          "default": 24,
          "minimum": 1,
          "description": "Fallback max number of blocks per API request when the model context window cannot be read."
        },
        "marklingo.translation.maxContextUsageRatio": {
          "type": "number",
          "default": 0.5,
          "minimum": 0.1,
          "maximum": 0.9,
          "description": "Max share of the model context window used for each translation request prompt when context length is available."
        },
        "marklingo.translation.requestMode": {
          "type": "string",
          "default": "auto",
          "enum": [
            "auto",
            "chatJson",
            "translationModel"
          ],
          "enumDescriptions": [
            "Use the verified provider/model behavior when available; otherwise use the standard request path.",
            "Use the standard structured-output request path.",
            "Use smaller Markdown batches for models that need a simpler translation request."
          ],
          "description": "Advanced translation request mode. Normally managed by MarkLingo Settings Save and Verify."
        },
        "marklingo.translation.translationModelMaxBlocksPerRequest": {
          "type": "number",
          "default": 12,
          "minimum": 1,
          "maximum": 24,
          "description": "Maximum number of Markdown blocks per request for the smaller-batch request path."
        },
        "marklingo.translation.translationModelConcurrency": {
          "type": "number",
          "default": 1,
          "minimum": 1,
          "maximum": 4,
          "description": "Maximum concurrent requests for the smaller-batch request path."
        },
        "marklingo.translation.translationModelMaxOutputTokens": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "maximum": 32768,
          "description": "Maximum output tokens per request for the smaller-batch request path. Use 0 to estimate from the model context window."
        },
        "marklingo.translation.targetLanguage": {
          "type": "string",
          "default": "简体中文",
          "enum": [
            "简体中文",
            "繁体中文",
            "English",
            "日本語",
            "한국어",
            "Français",
            "Español",
            "Deutsch",
            "Custom..."
          ],
          "description": "Target language for translation (shown in onboarding on first use)."
        },
        "marklingo.translation.targetLanguageCustom": {
          "type": "string",
          "default": "",
          "description": "Custom target language used when targetLanguage is Custom..."
        },
        "marklingo.translation.systemPrompt": {
          "type": "string",
          "default": "",
          "description": "Advanced override for the base system prompt template (not shown in the MarkLingo settings panel). Leave empty to use the extension default. Use {targetLanguage} as the target language placeholder. Editing this can break Markdown and placeholder protection, so most users should use Custom Instructions instead."
        },
        "marklingo.translation.customPrompt": {
          "type": "string",
          "default": "",
          "description": "Custom instructions appended after the built-in translation prompt, such as terminology or style rules. The base Markdown and placeholder protection always applies."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "bundle": "esbuild ./out/extension.js --bundle --platform=node --format=esm --external:vscode --sourcemap --outfile=dist/extension.js",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && node --test test/*.test.js",
    "test:vscode": "npm run package && node scripts/run-vscode-tests.mjs",
    "test:vscode:clean": "rm -rf .vscode-test && npm run test:vscode",
    "check:integration": "npm run check && npm run test:vscode",
    "check": "npm run compile && npm test",
    "benchmark:postprocessing": "npm run compile && node scripts/benchmark-postprocessing.mjs",
    "sync:model-pricing": "npm run compile && node scripts/sync-model-pricing.mjs",
    "preview:settings": "node scripts/preview-settings.mjs",
    "dev:vscode": "npm run package && node scripts/open-vscode-dev.mjs",
    "package": "npm run compile && npm run bundle",
    "package:dry": "npm run package && npx --no-install vsce ls --tree",
    "vsix": "npm run check && npx --no-install vsce package",
    "vscode:prepublish": "npm run package"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/vscode": "^1.90.0",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.2.2",
    "esbuild": "^0.25.0",
    "ovsx": "^1.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "remark-frontmatter": "^5.0.0",
    "remark-gfm": "^4.0.1",
    "remark-parse": "^11.0.0",
    "unified": "^11.0.5",
    "unist-util-visit": "^5.0.0"
  }
}
